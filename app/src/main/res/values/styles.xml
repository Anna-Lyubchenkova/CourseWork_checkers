<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="transparent">@color/transparent</item>
        <item name="middlegrey">@color/middlegrey</item>
        <item name="yellow">@color/yellow</item>
        <item name="white">@color/white</item>

        <!-- game colors -->
        <item name="cellWhite">@color/cellWhite</item>
        <item name="cellBlack">@color/cellBlack</item>
        <item name="cellSelect">@color/cellSelect</item>
        <item name="cellOption">@color/cellOption</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Base application theme. -->
    <style name="AppThemeDark" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryDarkSecond</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkSecond</item>
        <item name="colorAccent">@color/colorAccentSecond</item>

        <item name="transparent">@color/transparentSecond</item>
        <item name="middlegrey">@color/middlegreySecond</item>
        <item name="yellow">@color/yellowSecond</item>
        <item name="white">@color/whiteSecond</item>

        <!-- game colors -->
        <item name="cellWhite">@color/cellWhiteSecond</item>
        <item name="cellBlack">@color/cellBlackSecond</item>
        <item name="cellSelect">@color/cellSelectSecond</item>
        <item name="cellOption">@color/cellOptionSecond</item>

    </style>

    <style name="AppThemeDark.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppThemeDark.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppThemeDark.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="WinDialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">fill_parent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">adjustPan|stateUnspecified</item>
    </style>


    <style name="RatingBarStyle" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/ratingbar</item>
    </style>

    <style name="RatingBar" parent="RatingBarStyle">
        <item name="android:minHeight">50dp</item>
        <item name="android:maxHeight">50dp</item>
        <item name="android:layout_height">50dp</item>
    </style>

</resources>
